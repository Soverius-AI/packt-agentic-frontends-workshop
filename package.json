{
  "name": "packt-agentic-frontends-workshop",
  "version": "0.1.0",
  "private": true,
  "description": "Reference system and teaching workspace for the Packt Hands-On Agentic Frontends workshop.",
  "type": "module",
  "packageManager": "pnpm@11.19.0",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "test": "pnpm --filter @packt-workshop/contracts test -- --run && pnpm --filter @packt-workshop/compliance-service test -- --run && pnpm --filter @packt-workshop/coordinator-service test -- --run && pnpm --filter angular-host test --watch=false",
    "check": "pnpm build && pnpm test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "dev:compliance": "pnpm --filter @packt-workshop/compliance-service dev",
    "dev:coordinator": "pnpm --filter @packt-workshop/coordinator-service dev",
    "dev:angular": "pnpm --filter angular-host start",
    "dev:react": "pnpm --filter react-host dev"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "prettier": "^3.8.1",
    "typescript": "~6.0.2",
    "vitest": "^4.0.8"
  }
}
