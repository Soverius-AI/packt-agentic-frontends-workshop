# 05 — Frontend tools

For these webinar branches, use the [current milestone-5 guide](../../webinar/milestone-05.md).
It prepares the UI methods and Angular-local schemas, and leaves reactive context,
two direct frontend-tool registrations and the Mastra prompt change to the presenter.
The old seven-tool WorkshopHost implementation is no longer used.
